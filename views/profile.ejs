<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/profile.css">
    <link href="./output.css" rel="stylesheet">
</head>
<body>

    

    <div class="logout">
        <a href="/logout">Logout</a>
    </div>

    <div class="nav">

        <div class="profile-img">
            <img src="/images/uploads/<%= user.profilepic %>" alt="">
        </div>
        
        <h1>Hello , <%= user.username %></h1>
    </div>

    <h3>You can now create a new post </h3>



    <form action="/post" method="post">
        <textarea name="content" placeholder="Write what in your mind ?"></textarea>
        <input type="submit" value="Create post">    
    </form>

    <h4>Your Posts.</h4>

    <div class="container w-full mt-8 flex flex-wrap gap-5 bg-red-600">
    <% user.posts.forEach((post)=>{ %>
            <div class="post bg-gray-900 p-4 w-72 border-2 border-black/50 rounded-lg">
                <h5 class="mb-2 text-lg text-indigo-700"><%= user.name %></h5>
                <p class="text-xs"><%= post.content %>.</p>

                <span><%= post.likes.length %>-Likes</span>
                <div class="anc flex mt-5 justify-between text-sm">
                    <a href="/like/<%= post._id %>" class="like text-indigo-700 no-underline">
                        <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
                    </a>
                    <a href="/edit/<%= post._id %>" class="edit text-gray-500 no-underline">Edit</a>
                    <a href="/delete/<%= post._id %>" class="delete text-red-600">Delete</a>
                </div>
            </div> 
        <% }) %>       
    </div>
</body>
</html>